# DhammaAI 项目规范

## 项目目标

DhammaAI 是基于 Quartz v5 的个人博客与数字花园，发布于 `guanzhang.dhammaai.com`。所有修改以稳定阅读、清晰导航和长期可维护为优先，不为单一页面或单一设备制造临时特例。

## 单一事实源

- 本文件是本项目**开发与验收规范**的唯一事实源。
- 页面结构与插件布局以 `quartz.config.yaml` 为准。
- 视觉 token、版心与响应式规则以 `quartz/styles/custom.scss` 为准。
- 可执行回归约束以测试文件为准；文档不复制实现细节。
- **项目中控**（定位、边界、状态）在 `PROJECT.md`（真文件，仓内指针，可进 CI；指向 `obsidian-projects/DhammaAI/AGENT.md`；Obsidian `1_Projects/DhammaAI.md` 软链到同一文件）。中控可摘要本文件，但不得另立一套冲突的布局规则。
- 系统层混合项目协议见 Vault `1_Projects/AGENT.md`「混合项目三层互通」。

## 会话开闭（与中控 / Obsidian / 记忆互通）

### 开场

1. 读 `PROJECT.md`（中控：状态与边界）。
2. 再读本文件（工程门槛）。
3. 若延续旧基线：`nmem m search "dhamma-garden"`。

### 收尾（可交付完成后必做）

1. 完成本文件要求的 check / test / build / 推送与线上验收（若适用）。
2. 运行互通收尾（刷新中控基线块 + 更新记忆）：

   ```bash
   ./scripts/project-closeout --summary "一句话说明本次交付"
   # 或 check-only：
   ./scripts/project-closeout --check-only
   ```

3. 不得只改代码仓文档而不更新中控状态；Obsidian 无需另拷——中控软链即入口。
4. 仅当 Active/Paused/Completed 生命周期变化时，才改 `PROJECTS-REGISTRY.yml` 与 Vault 内 symlink 位置。

## 三端兼容硬门槛

任何布局、组件、排版或视觉调整，都必须同时兼容桌面端、iPad 横屏、iPad 竖屏和手机端。只在桌面端正常不算完成，不得上线。

### 响应式结构

- 桌面端：保留 Quartz 左栏、中栏、右栏三栏结构；三个栏目页的阅读起点必须一致，不得随内容长短漂移。
- iPad 横屏与 iPad 竖屏：使用横向顶栏和单列阅读区；右栏辅助信息移到正文之后，不得挤压正文。
- 手机端：使用品牌、导航、工具分层的顶栏和单列正文；关系图谱 / TOC / 反链等右栏内容移到正文之后（下滑可见），不得挤占首屏侧栏。
- 触屏端的主要链接与按钮点击区域不得小于 `44 × 44 CSS px`。

### 标准验收视口

- 桌面端：`1440 × 900`、`1280 × 800`
- iPad 横屏：`1194 × 834`
- iPad 竖屏：`834 × 1194`
- 手机端：`430 × 932`、`390 × 844`

### 必检页面

- `/`
- `/关于/`
- `/佛法修学/`
- `/ai实践/`
- 本次修改直接影响的其他页面或交互状态

### 验收标准

- 无横向滚动、元素重叠、裁切、竖排错位或不可点击控件。
- 导航、搜索、主题切换和正文阅读顺序清晰可用。
- 同类型页面的标题、正文、分隔线与辅助区域保持统一几何关系。
- 内容长度不得影响栏目页的横向起点或网格宽度。
- 涉及颜色、边框或对比度时，同时检查亮色与暗色主题。
- 响应式断点前后不得出现结构跳变、空列或侧栏占位残留。

## 修改与验证流程

1. 修改前先建立失败的自动测试或可执行几何验收。
2. 优先修复布局规则的共同根因，禁止为“关于”“佛法修学”或“AI 实践”分别堆叠补丁。
3. 本地必须通过：

   ```bash
   npm run check
   npm test
   npx quartz build
   ```

4. 使用真实浏览器按标准视口检查必检页面；布局改动必须同时保存像素测量或截图证据。
5. 推送 `v5` 后等待 GitHub Actions 与 Cloudflare Pages 完整成功，再对线上域名重复核心验收。

## 变更纪律

- 保持改动小而可回滚，一个行为目标对应一个提交。
- 不修改 Quartz 生成目录或插件缓存来掩盖源代码问题。
- 不以隐藏整个桌面功能作为移动端适配的默认手段；仅可隐藏在小屏确属次要且不影响核心任务的辅助模块。
- 新规则应进入现有 SSOT 与回归测试，不创建平行规范。
